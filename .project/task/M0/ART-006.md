# Art Task: Documents 页面美术资产

**ID**: ART-006  
**Type**: art  
**Status**: art_done  
**Priority**: P2  
**Created**: 2026-03-21

---

## 任务概述

产出 Documents 文档中心页面的美术资产，包括文档树形结构、文档预览等。

## 资产清单

### 1. 树形结构
| 资产 | 规格 | 格式 | 输出路径 |
|:-----|:-----|:-----|:---------|
| 树节点样式 | 左侧缩进 20px | CSS | `assets/css/components.css` |
| 展开/折叠图标 | chevron-right | SVG | `assets/icons/chevron-right.svg` |
| 展开动画 | rotate 0 → 90 度 | CSS | `assets/css/animations.css` |
| 节点悬停效果 | 背景色变化 | CSS | `assets/css/animations.css` |
| 文件夹图标 | 黄色文件夹 | SVG | `assets/icons/folder.svg` |
| 文档图标 | 文件类型区分 | SVG | `assets/icons/file-*.svg` |

### 2. 文档预览 Modal
| 资产 | 规格 | 格式 | 输出路径 |
|:-----|:-----|:-----|:---------|
| Modal 遮罩 | 背景 rgba(0,0,0,0.5) | CSS | `assets/css/components.css` |
| Modal 容器 | 宽 80vw，高 70vh | CSS | `assets/css/components.css` |
| Modal 动画 | scale 0.95 → 1, 0.25s | CSS | `assets/css/animations.css` |
| 关闭按钮 | 右上角 X | SVG | `assets/icons/x.svg` |

### 3. 搜索组件
| 资产 | 规格 | 格式 | 输出路径 |
|:-----|:-----|:-----|:---------|
| 搜索框样式 | 圆角 8px | CSS | `assets/css/components.css` |
| 搜索图标 | 放大镜 | SVG | `assets/icons/search.svg` |
| 搜索结果高亮 | 黄色背景标记 | CSS | `assets/css/components.css` |

### 4. 筛选器
| 资产 | 规格 | 格式 | 输出路径 |
|:-----|:-----|:-----|:---------|
| 筛选下拉框 | 项目/类型筛选 | CSS | `assets/css/components.css` |
| 筛选标签 | 已选筛选条件 | CSS | `assets/css/components.css` |

---

## 验收标准

- [ ] 树形结构缩进清晰
- [ ] 展开/折叠动画流畅
- [ ] Modal 弹出动画自然
- [ ] 搜索结果关键词高亮可见
- [ ] 不同文件类型图标可区分

---

## 技术约束

- 树形结构使用 `<ul>/<li>` + CSS 实现
- Modal 使用 fixed 定位居中
- 搜索高亮使用 `<mark>` 标签 + CSS
- 文件图标建议：MD (蓝)、JSON (橙)、TXT (灰)

---

## 依赖关系

- **Hard 依赖**: 无
- **被依赖**: Engineer 实现 Documents 页面

---

## 备注

参考 `requirements/07-page-design.md` 获取功能详情。
