# Art Task: Projects 详情页美术资产

**ID**: ART-002  
**Type**: art  
**Status**: art_done  
**Priority**: P0  
**Created**: 2026-03-21

---

## 任务概述

产出 Projects 详情页的所有美术资产，包括项目概览卡片、Tab 切换、Agent 状态、任务列表、测试结果、Bugs、版本历史等组件。

## 资产清单

### 1. 项目概览卡片
| 资产 | 规格 | 格式 | 输出路径 |
|:-----|:-----|:-----|:---------|
| 概览卡片样式 | 圆角 12px，阴影 | CSS | `assets/css/components.css` |
| Phase 标签 | 9 种颜色对应 P0-P8 | CSS | `assets/css/components.css` |
| 进度条样式 | 渐变 #3B82F6 → #10B981 | CSS | `assets/css/components.css` |
| 进度条动画 | width transition 0.8s | CSS | `assets/css/animations.css` |

### 2. Tab 切换组件
| 资产 | 规格 | 格式 | 输出路径 |
|:-----|:-----|:-----|:---------|
| Tab 栏样式 | 高度 44px | CSS | `assets/css/components.css` |
| Tab 切换动画 | opacity 0.2s ease | CSS | `assets/css/animations.css` |
| Tab 下划线 | 2px #3B82F6 | CSS | `assets/css/components.css` |

### 3. Agent 状态组件
| 资产 | 规格 | 格式 | 输出路径 |
|:-----|:-----|:-----|:---------|
| Agent 头像占位符 | 40x40 圆形 | SVG | `assets/icons/agent-avatar.svg` |
| Agent 卡片样式 | 可折叠 | CSS | `assets/css/components.css` |
| 折叠展开动画 | max-height 0.3s | CSS | `assets/css/animations.css` |

### 4. 任务列表组件
| 资产 | 规格 | 格式 | 输出路径 |
|:-----|:-----|:-----|:---------|
| 任务状态图标 | 6 种状态 | SVG | `assets/icons/status-*.svg` |
| 表格样式 | 表头 48px，行高 48px | CSS | `assets/css/components.css` |
| 行悬停效果 | background 0.2s | CSS | `assets/css/animations.css` |

### 5. 测试结果组件
| 资产 | 规格 | 格式 | 输出路径 |
|:-----|:-----|:-----|:---------|
| 测试统计卡片 | 4 种指标 | CSS | `assets/css/components.css` |
| 测试报告表格 | 斑马纹 | CSS | `assets/css/components.css` |
| 状态标签 | 通过/失败 | CSS | `assets/css/components.css` |

### 6. Bug 列表组件
| 资产 | 规格 | 格式 | 输出路径 |
|:-----|:-----|:-----|:---------|
| Bug 优先级图标 | High/Medium/Low | SVG | `assets/icons/bug-*.svg` |
| Bug 列表样式 | 表格布局 | CSS | `assets/css/components.css` |

### 7. 版本历史组件
| 资产 | 规格 | 格式 | 输出路径 |
|:-----|:-----|:-----|:---------|
| 版本标签样式 | 圆角胶囊 | CSS | `assets/css/components.css` |
| 版本卡片样式 | 折叠展开 | CSS | `assets/css/components.css` |

---

## 验收标准

- [ ] Tab 切换动画流畅（0.2s）
- [ ] Agent 卡片折叠/展开动画完整
- [ ] 任务状态图标 6 种可区分
- [ ] 测试结果表格斑马纹正确
- [ ] Bug 优先级图标 High🔴/Medium🟡/Low🔵 区分明显
- [ ] 进度条渐变动画从 0 到目标值

---

## 技术约束

- 任务状态图标：🔄 waiting, ⏳ in_progress, ✅ done, ❌ cancelled, ⏸ paused, 🔵 assigned
- Bug 优先级颜色：High #EF4444, Medium #F59E0B, Low #3B82F6
- 表格使用 CSS Grid 或 Flexbox 实现
- 折叠动画使用 max-height + overflow hidden

---

## 依赖关系

- **Hard 依赖**: ART-001（Dashboard 基础样式）
- **被依赖**: Engineer 实现 Projects 详情页

---

## 备注

测试结果数据来源：`test-results/`  
Bug 数据来源：`test-results/bugs/`  
参考 `visual-design/pages/projects-page.svg` 获取详细布局。
